name: designsystem
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  datepicker:
    path: ../datepicker
  utils:
    path: ../utils
  collection:
  flutter_spinkit:
  pinput: ^4.0.0
  flip_card: ^0.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true