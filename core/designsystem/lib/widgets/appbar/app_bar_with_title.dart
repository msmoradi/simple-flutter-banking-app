import 'package:flutter/material.dart';

class AppBarWithTitle extends StatelessWidget {
  const AppBarWithTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
